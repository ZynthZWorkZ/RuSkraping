<Window x:Class="RuSkraping.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RuSkraping"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="RuSkraping" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        BorderBrush="#e9182b"
        BorderThickness="0"
        WindowStyle="None"
        AllowsTransparency="True">
    
    <Window.Resources>
        <!-- Metallic Gradients -->
        <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A1A1A" Offset="0"/>
            <GradientStop Color="#121212" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ControlBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#2A2A2A" Offset="0"/>
            <GradientStop Color="#1E1E1E" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonHoverBackground" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#e9182b" Offset="0"/>
            <GradientStop Color="#c41524" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="AccentBrush" Color="#e9182b"/>
        <SolidColorBrush x:Key="GreenAccentBrush" Color="#4fbf37"/>
        <SolidColorBrush x:Key="BlueAccentBrush" Color="#142ae0"/>
        
        <!-- Styles -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundGradient}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for Search, Pause, Stop buttons -->
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ControlBackgroundGradient}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundGradient}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundGradient}"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#e9182b"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="20" Height="20" CornerRadius="5" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
                                <Path x:Name="CheckMark" Width="12" Height="12" Stretch="Uniform" Fill="{StaticResource AccentBrush}" 
                                      Data="M0,5.5 L2,3.5 L6,7.5 L14,0.5 L16,2.5 L6,11.5 Z" 
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="5,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="{StaticResource GreenAccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="3">
                                <Grid>
                                    <Border x:Name="PART_Indicator" 
                                            Background="{TemplateBinding Foreground}"
                                            CornerRadius="3"
                                            HorizontalAlignment="Left">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#4fbf37" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                        </Border.Effect>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Loading Spinner Style -->
        <Style x:Key="LoadingSpinnerStyle" TargetType="ProgressBar">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="IsIndeterminate" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

         <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ScrollBar Styles -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb x:Name="Thumb" Background="{StaticResource AccentBrush}" 
                                       BorderThickness="0" Margin="0,2">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="12"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" 
                                     Grid.Column="1" Grid.Row="0"
                                     Orientation="Vertical"
                                     Value="{TemplateBinding VerticalOffset}"
                                     Maximum="{TemplateBinding ScrollableHeight}"
                                     ViewportSize="{TemplateBinding ViewportHeight}"
                                     Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                     Grid.Column="0" Grid.Row="1"
                                     Orientation="Horizontal"
                                     Value="{TemplateBinding HorizontalOffset}"
                                     Maximum="{TemplateBinding ScrollableWidth}"
                                     ViewportSize="{TemplateBinding ViewportWidth}"
                                     Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource WindowBackgroundGradient}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                 <Border Background="#1A1A1A" CornerRadius="16,16,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="RuTracker Search" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="20,10" VerticalAlignment="Center" Grid.Column="0"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="MinimizeButton" Click="MinimizeButton_Click" Style="{StaticResource ActionButtonStyle}" Width="30" Height="30" Margin="5,0">
                                <fa:FontAwesome Icon="Minus" Foreground="{StaticResource AccentBrush}"/>
                            </Button>
                            <Button x:Name="CloseButton" Click="CloseButton_Click" Style="{StaticResource ActionButtonStyle}" Width="30" Height="30">
                                <fa:FontAwesome Icon="Close" Foreground="{StaticResource AccentBrush}"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search Section -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SearchTextBox" Grid.Column="0" Margin="0,0,10,0" 
                                VerticalContentAlignment="Center" FontSize="14"/>
                        <Button x:Name="SearchButton" Grid.Column="1" Margin="0,0,10,0" Click="SearchButton_Click" Style="{StaticResource ActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <fa:FontAwesome Icon="Search" Margin="0,0,5,0" Foreground="#FFFFFF"/>
                                <TextBlock Text="Search" Foreground="#FFFFFF"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="PauseButton" Grid.Column="2" Margin="0,0,10,0" Click="PauseButton_Click" IsEnabled="False" Style="{StaticResource ActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <fa:FontAwesome Icon="Pause" Margin="0,0,5,0" Foreground="#FFFFFF"/>
                                <TextBlock Text="Pause" Foreground="#FFFFFF"/>
                            </StackPanel>
                        </Button>
                        <Button x:Name="StopButton" Grid.Column="3" Margin="0,0,10,0" Click="StopButton_Click" IsEnabled="False" Style="{StaticResource ActionButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <fa:FontAwesome Icon="Stop" Margin="0,0,5,0" Foreground="#FFFFFF"/>
                                <TextBlock Text="Stop" Foreground="#FFFFFF"/>
                            </StackPanel>
                        </Button>
                        <CheckBox x:Name="UseCookiesCheckBox" Grid.Column="4" Content="Use Cookies" 
                                VerticalAlignment="Center" IsChecked="True"/>
                        <CheckBox x:Name="FetchImagesCheckBox" Grid.Column="5" Content="Fetch Images" 
                                VerticalAlignment="Center" IsChecked="False" Margin="10,0,0,0"/>
                    </Grid>
                </StackPanel>

                <!-- Results Section -->
                <ListView x:Name="ResultsListView" Grid.Row="1" Margin="0,0,0,20">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Image" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ImageUrl}" Width="60" Height="60" Stretch="Uniform"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Title" Width="400" DisplayMemberBinding="{Binding Title}"/>
                            <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                            <GridViewColumn Header="Topic ID" Width="100" DisplayMemberBinding="{Binding TopicId}"/>
                            <GridViewColumn Header="Details" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button x:Name="DetailsButton" Click="DetailsButton_Click" Style="{StaticResource ActionButtonStyle}" Width="30" Height="30">
                                            <fa:FontAwesome Icon="Info" Foreground="#FFFFFF"/>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="GetMagnetButton" Margin="0,0,10,0" Click="GetMagnetButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Magnet" Margin="0,0,5,0" Foreground="#FFFFFF"/>
                            <TextBlock Text="Get Magnet Link" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="OpenMagnetButton" Click="OpenMagnetButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="ExternalLink" Margin="0,0,5,0" Foreground="#FFFFFF"/>
                            <TextBlock Text="Open Magnet" Foreground="#FFFFFF"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Status Bar -->
            <StatusBar Grid.Row="2" Background="#1A1A1A">
                <StatusBarItem>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="StatusText" Text="Ready" Foreground="#CCCCCC"/>
                    </StackPanel>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Border>
</Window>
