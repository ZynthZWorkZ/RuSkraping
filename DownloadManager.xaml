<Window x:Class="RuSkraping.DownloadManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RuSkraping"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="RUSKTorrent Manager" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        BorderThickness="0"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Modern Color Scheme -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1E1E2D"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#2B2B3F"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#6C5DD3"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF4B4B"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#50CD89"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FFC700"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#A1A5B7"/>

        <!-- Gradients -->
        <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1E1E2D" Offset="0"/>
            <GradientStop Color="#151521" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ButtonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6C5DD3" Offset="0"/>
            <GradientStop Color="#5346B0" Offset="1"/>
        </LinearGradientBrush>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ButtonGradient}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListView Style -->
        <Style TargetType="ListView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- ListViewItem Style -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#323248"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#323248"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ProgressBar Style -->
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Height" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <Border x:Name="PART_Track">
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        HorizontalAlignment="Left"
                                        CornerRadius="3"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource WindowBackgroundGradient}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="16,16,0,0" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <fa:FontAwesome Icon="Download" Foreground="{StaticResource AccentBrush}" FontSize="24" Margin="0,0,15,0"/>
                        <TextBlock Text="BitTorrent Download Manager" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,10">
                        <Button x:Name="MinimizeButton" Click="MinimizeButton_Click" Width="40" Height="40" Margin="0,0,10,0"
                                Background="Transparent" BorderThickness="0">
                            <fa:FontAwesome Icon="Minus" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                        <Button x:Name="CloseButton" Click="CloseButton_Click" Width="40" Height="40"
                                Background="Transparent" BorderThickness="0">
                            <fa:FontAwesome Icon="Close" Foreground="{StaticResource DangerBrush}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Toolbar -->
            <Border Grid.Row="1" Background="{StaticResource SecondaryBrush}" Padding="20,15">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="AddTorrentButton" Click="AddTorrentButton_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Plus" Margin="0,0,8,0"/>
                            <TextBlock Text="Add Torrent"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="AddMagnetButton" Click="AddMagnetButton_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Magnet" Margin="0,0,8,0"/>
                            <TextBlock Text="Add Magnet"/>
                        </StackPanel>
                    </Button>
                    <Border Width="1" Height="30" Background="{StaticResource TextSecondaryBrush}" Opacity="0.3" Margin="10,0"/>
                    <Button x:Name="StartButton" Click="StartButton_Click" Margin="10,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Play" Margin="0,0,8,0" Foreground="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="Start"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="PauseButton" Click="PauseButton_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Pause" Margin="0,0,8,0" Foreground="{StaticResource WarningBrush}"/>
                            <TextBlock Text="Pause"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="StopButton" Click="StopButton_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Stop" Margin="0,0,8,0" Foreground="{StaticResource DangerBrush}"/>
                            <TextBlock Text="Stop"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="RemoveButton" Click="RemoveButton_Click" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="Trash" Margin="0,0,8,0" Foreground="{StaticResource DangerBrush}"/>
                            <TextBlock Text="Remove"/>
                        </StackPanel>
                    </Button>
                    <Border Width="1" Height="30" Background="{StaticResource TextSecondaryBrush}" Opacity="0.3" Margin="0,0,10,0"/>
                    <Button x:Name="ViewDetailsButton" Click="ViewDetailsButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <fa:FontAwesome Icon="InfoCircle" Margin="0,0,8,0" Foreground="{StaticResource AccentBrush}"/>
                            <TextBlock Text="View Details"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Torrents List -->
            <ListView x:Name="TorrentsListView" Grid.Row="2" Margin="20" SelectionChanged="TorrentsListView_SelectionChanged" MouseDoubleClick="TorrentsListView_MouseDoubleClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Torrent Name and State -->
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" 
                                          Foreground="{StaticResource TextPrimaryBrush}"/>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBlock Text="{Binding StateText}" FontSize="12" 
                                              Foreground="{StaticResource AccentBrush}" Margin="0,0,15,0"/>
                                    <TextBlock Text="{Binding ProgressFormatted}" FontSize="12" FontWeight="Bold"
                                              Foreground="{StaticResource SuccessBrush}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Progress Bar -->
                            <ProgressBar Grid.Row="1" Value="{Binding Progress}" Maximum="100" Margin="0,0,0,8"/>

                            <!-- Stats -->
                            <Grid Grid.Row="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <fa:FontAwesome Icon="ArrowDown" FontSize="12" Foreground="{StaticResource SuccessBrush}" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding DownloadSpeedFormatted}" FontSize="12" 
                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <fa:FontAwesome Icon="ArrowUp" FontSize="12" Foreground="{StaticResource WarningBrush}" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding UploadSpeedFormatted}" FontSize="12" 
                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <fa:FontAwesome Icon="Users" FontSize="12" Foreground="{StaticResource AccentBrush}" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding PeersText}" FontSize="12" 
                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="3" Orientation="Horizontal">
                                    <fa:FontAwesome Icon="ClockOutline" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding ETAFormatted}" FontSize="12" 
                                              Foreground="{StaticResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Status Bar -->
            <Border Grid.Row="3" Background="{StaticResource PrimaryBrush}" CornerRadius="0,0,16,16" Padding="20,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="StatusText" Text="Ready" Foreground="{StaticResource TextSecondaryBrush}"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <fa:FontAwesome Icon="ArrowDown" Foreground="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TotalDownloadSpeed" Text="0 KB/s" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,20,0"/>
                        <fa:FontAwesome Icon="ArrowUp" Foreground="{StaticResource WarningBrush}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TotalUploadSpeed" Text="0 KB/s" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
