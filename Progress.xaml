<Window x:Class="RuSkraping.Progress"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RuSkraping"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Progress" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        BorderBrush="#e9182b"
        BorderThickness="1"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <LinearGradientBrush x:Key="WindowBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A1A1A" Offset="0"/>
            <GradientStop Color="#121212" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ProgressBackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#2A2A2A" Offset="0"/>
            <GradientStop Color="#1E1E1E" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="ProgressForegroundGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#e9182b" Offset="0"/>
            <GradientStop Color="#ff1f33" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="16" Background="{StaticResource WindowBackgroundGradient}" BorderBrush="{StaticResource ProgressBackgroundGradient}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
                <Border Background="#1A1A1A" CornerRadius="16,16,0,0">
                    <TextBlock Text="Progress" FontSize="18" FontWeight="Bold" Foreground="#e9182b" Margin="20,10" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Status Text -->
                <TextBlock x:Name="StatusText" Text="Processing..." Grid.Row="0" FontSize="16" Margin="0,0,0,15"/>

                <!-- Progress Bar -->
                <Border Grid.Row="1" Background="{StaticResource ProgressBackgroundGradient}" CornerRadius="10" Height="20" Margin="0,0,0,10">
                    <Grid>
                        <Border x:Name="ProgressIndicator" 
                                Background="#e9182b"
                                CornerRadius="10"
                                HorizontalAlignment="Left"
                                Width="0">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="#e9182b" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                        </Border>
                    </Grid>
                </Border>

                <!-- Progress Percentage -->
                <TextBlock x:Name="ProgressText" Text="0%" Grid.Row="2" HorizontalAlignment="Center" FontSize="14" Foreground="#CCCCCC"/>
            </Grid>
        </Grid>
    </Border>
</Window>
